using UnityEngine;
using System.Collections;

public class ButtonCounter : MonoBehaviour
{
    void Update()
    {//every frame
        if (Input.GetKeyDown(KeyCode.Escape))           //Change "Escape" to whatever button you want it to be.
        {//if Escape is pressed

            //code here
        }
    }
}

